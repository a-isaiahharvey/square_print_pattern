name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: cd ./CSharp/SquarePrintPattern && dotnet restore 
    - name: Build
      run: cd ./CSharp/SquarePrintPattern && dotnet build --no-restore
    - name: Test
      run: cd ./CSharp/SquarePrintPattern && dotnet test --no-build --verbosity normal
